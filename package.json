{
  "name": "space-maze",
  "type": "module",
  "version": "1.0.0",
  "description": "Space maze explorer built with Astro and Three.js.",
  "packageManager": "bun@1.3.10",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy": "bun run build && wrangler pages deploy dist --project-name space-maze",
    "deploy:production": "bun run build && wrangler pages deploy dist --project-name space-maze --branch production",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.18.0",
    "three": "^0.183.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@astrojs/cloudflare": "^12.6.12",
    "typescript": "^5.9.3",
    "wrangler": "^4.71.0"
  }
}
